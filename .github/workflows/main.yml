name: Build, Push, Deploy to EKS (Canada)

on:
  push:
    branches: [ "main" ]

env:
  AWS_REGION: "ca-central-1"
  EKS_CLUSTER_NAME: "mycluster"
  HELM_CHART_NAME: "test"
  ECR_REPOSITORY: "755583968331.dkr.ecr.ca-central-1.amazonaws.com/myapprepo"
  # Removed the substr filter from env and will handle it in the step

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # --- 1. Checkout Code ---
      - name: Checkout
        uses: actions/checkout@v4

      # --- 2. Configure AWS for Canada ---
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # --- 3. Create ECR Repo (if missing) ---
      - name: Ensure ECR Repository
        run: |
          aws ecr describe-repositories --repository-names myapprepo || \
          aws ecr create-repository --repository-name myapprepo

      # --- 4. Login to ECR ---
      - name: ECR Login
        uses: aws-actions/amazon-ecr-login@v2

      # --- 5. Build & Push ---
      - name: Build and Push
        id: build-push
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          docker build -t ${{ env.ECR_REPOSITORY }}:$SHORT_SHA .
          docker push ${{ env.ECR_REPOSITORY }}:$SHORT_SHA
          echo "SHORT_SHA=$SHORT_SHA" >> $GITHUB_ENV

      # --- 6. Setup Kubernetes Tools ---
      - name: Install Tools
        uses: azure/setup-helm@v3

      # --- 7. Configure EKS Access ---
      - name: Configure EKS
        run: |
          aws eks update-kubeconfig \
            --region ${{ env.AWS_REGION }} \
            --name ${{ env.EKS_CLUSTER_NAME }}

      # --- 8. Deploy with Helm ---
      - name: Helm Upgrade
        run: |
          helm upgrade --install \
            ${{ env.HELM_CHART_NAME }} \
            ./test \                  # Changed to your actual chart path
            --namespace default \
            --set image.repository=${{ env.ECR_REPOSITORY }} \
            --set image.tag=${{ env.SHORT_SHA }} \
            --atomic
